<TreeView x:Class="RedBlackTreeVisuals.RedBlackTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedBlackTreeVisuals"
             mc:Ignorable="d" 
             Height="450" Width="800"
             ItemsSource="{Binding Root}">
    <TreeView.ItemTemplate>
        <HierarchicalDataTemplate>
            <StackPanel Orientation="Horizontal">
                <Image Width="20" Margin="3" Source="{Binding Type, Converter={StaticResource NodeTypeToImageConverter }}" />
                <TextBlock VerticalAlignment="Center" Text="{Binding Value}" />
            </StackPanel>
        </HierarchicalDataTemplate>
    </TreeView.ItemTemplate>
</TreeView>
